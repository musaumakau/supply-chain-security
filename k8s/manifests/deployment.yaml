apiVersion: apps/v1
kind: Deployment
metadata:
  name: supply-chain-demo
  namespace: default
  labels:
    app: supply-chain-demo
spec:
  replicas: 5
  selector:
    matchLabels:
      app: supply-chain-demo
  template:
    metadata:
      labels:
        app: supply-chain-demo
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: supply-chain-demo
          # Pinned by digest, not tag -- this is the exact image that was
          # signed and attested by sign-attest.yml. A tag can be repointed
          # later; a digest cannot, which is the whole point of testing
          # Kyverno's verifyImages against it.
          image: europe-west1-docker.pkg.dev/stoked-citizen-455416-g4/supply-chain-security/supply-chain-demo@shasha256:116776c8ca1518c1ec08245ff241cf41b9f379854d747312da59eefe427698a9
          imagePullPolicy: IfNotPresent

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi

          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5

          livenessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10