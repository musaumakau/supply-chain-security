apiVersion: apps/v1
kind: Deployment
metadata:
  name: supply-chain-demo
  namespace: default
  labels:
    app: supply-chain-demo
spec:
  replicas: 5
  selector:
    matchLabels:
      app: supply-chain-demo
  template:
    metadata:
      labels:
        app: supply-chain-demo
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: supply-chain-demo
          # Pinned by digest, not tag -- this is the exact image that was
          # signed and attested by sign-attest.yml. A tag can be repointed
          # later; a digest cannot, which is the whole point of testing
          # Kyverno's verifyImages against it.
          image: europe-west1-docker.pkg.dev/stoked-citizen-455416-g4/supply-chain-security/supply-chain-demo@sha256:382f674b17f0e6eb53ce18d6123c26c988636b7f044d64978c801b05e4c82f87
          imagePullPolicy: IfNotPresent

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi

          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 5

          livenessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10