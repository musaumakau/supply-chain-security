apiVersion: v1
kind: Service
metadata:
  name: supply-chain-demo
  namespace: default
  labels:
    app: supply-chain-demo
spec:
  selector:
    app: supply-chain-demo
  ports:
    - port: 80
      targetPort: 8000 # matches deployment.yaml containerPort -- update together
  type: ClusterIP